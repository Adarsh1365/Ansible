
- name: Copy service file
  template:
    src: "{{ component }}.service.j2"
    dest: "/etc/systemd/system/{{ component }}.service"

- name: Deamon Reload
  systemd_service:
    name: "{{ component }}"
    daemon_reload: true
