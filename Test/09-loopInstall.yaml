- name: Looping
  hosts: frontend
  become: yes
  tasks:
    - name: install packages
      package:
        name: "{{ item }}"
        state: present
      loop:
        - nginx
        - mysql
        - git
    - name: start nginx
      service:
        name: nginx
        state: started
        enabled: yes
